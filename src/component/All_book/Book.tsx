import { useParams } from "react-router-dom";


const Book = () => {
    const {id}=useParams();
    console.log(id);
    return (
        <div>
            hdcjsfhbhj
        </div>
    );
};

export default Book;